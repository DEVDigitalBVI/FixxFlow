export default function HomePage() {
  return (
    <main className="shell">
      <section className="hero">
        <p className="eyebrow">FixxFlow</p>
        <h1>The foundation is ready.</h1>
        <p>
          Next.js, TypeScript, and Supabase are connected at the architecture
          level. Add your project keys to begin building product workflows.
        </p>
      </section>
    </main>
  );
}
