import { SubmitButton } from "@/components/ui/submit-button";
import Link from "next/link";
import { requestPasswordReset } from "../actions";
type Props = { searchParams: Promise<{ error?: string; success?: string }> };
export default async function ForgotPasswordPage({ searchParams }: Props) { const message = await searchParams; return <section className="auth-card" aria-labelledby="reset-heading"><h1 id="reset-heading">Reset your password</h1><p className="auth-intro">We’ll send a secure reset link to your work email.</p><form action={requestPasswordReset} className="stack">{message.error && <div className="alert alert-error" role="alert">{message.error}</div>}{message.success && <div className="alert alert-success" role="status">{message.success}</div>}<div className="field"><label htmlFor="email">Work email</label><input className="input" id="email" name="email" type="email" autoComplete="email" required /></div><SubmitButton className="button button-primary" pendingLabel="Sending link…">Send reset link</SubmitButton><Link href="/login">Return to sign in</Link></form></section>; }
