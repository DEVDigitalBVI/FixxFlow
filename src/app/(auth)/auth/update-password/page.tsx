import { SubmitButton } from "@/components/ui/submit-button";
import { updatePassword } from "../../actions";
type Props = { searchParams: Promise<{ error?: string }> };
export default async function UpdatePasswordPage({ searchParams }: Props) { const message = await searchParams; return <section className="auth-card" aria-labelledby="password-heading"><h1 id="password-heading">Choose a new password</h1><p className="auth-intro">Use at least 8 characters and avoid passwords used elsewhere.</p><form action={updatePassword} className="stack">{message.error && <div className="alert alert-error" role="alert">{message.error}</div>}<div className="field"><label htmlFor="password">New password</label><input className="input" id="password" name="password" type="password" minLength={8} autoComplete="new-password" required /></div><div className="field"><label htmlFor="passwordConfirmation">Confirm new password</label><input className="input" id="passwordConfirmation" name="passwordConfirmation" type="password" minLength={8} autoComplete="new-password" required /></div><SubmitButton className="button button-primary" pendingLabel="Updating password…">Update password</SubmitButton></form></section>; }
