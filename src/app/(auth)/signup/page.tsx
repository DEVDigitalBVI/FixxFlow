import Link from "next/link";
import { signUp } from "../actions";

type Props = { searchParams: Promise<{ error?: string }> };
export default async function SignupPage({ searchParams }: Props) {
  const message = await searchParams;
  return <section className="auth-card auth-card-wide" aria-labelledby="signup-heading"><div className="onboarding-progress" aria-label="Signup progress"><span className="is-current">1</span><i/><span>2</span><i/><span>3</span></div><span className="page-eyebrow">Start your workspace</span><h1 id="signup-heading">Create your FixxFlow account</h1><p className="auth-intro">Set up the administrator account you’ll use to configure your organization.</p><form action={signUp} className="stack">{message.error && <div className="alert alert-error" role="alert" tabIndex={-1}>{message.error}</div>}<div className="field"><label htmlFor="fullName">Your name</label><input className="input" id="fullName" name="fullName" autoComplete="name" required maxLength={120}/></div><div className="field"><label htmlFor="email">Work email</label><input className="input" id="email" name="email" type="email" autoComplete="email" required/><p className="form-note">We’ll send your verification link here.</p></div><div className="signup-fields"><div className="field"><label htmlFor="password">Password</label><input className="input" id="password" name="password" type="password" autoComplete="new-password" required minLength={8} aria-describedby="password-hint"/><p className="form-note" id="password-hint">At least 8 characters.</p></div><div className="field"><label htmlFor="passwordConfirmation">Confirm password</label><input className="input" id="passwordConfirmation" name="passwordConfirmation" type="password" autoComplete="new-password" required minLength={8}/></div></div><label className="check-field"><input type="checkbox" name="terms" value="accepted" required/><span>I agree to the Terms of Service and Privacy Policy.</span></label><button className="button button-primary" type="submit">Create account</button></form><p className="auth-switch">Already have an account? <Link href="/login">Sign in</Link></p></section>;
}
