import Link from "next/link";
import { signIn } from "../actions";
type Props = { searchParams: Promise<{ error?: string; success?: string }> };
export default async function LoginPage({ searchParams }: Props) { const message = await searchParams; return <section className="auth-card" aria-labelledby="login-heading"><h1 id="login-heading">Welcome back</h1><p className="auth-intro">Sign in to your organization workspace.</p><form action={signIn} className="stack">{message.error && <div className="alert alert-error" role="alert">{message.error}</div>}{message.success && <div className="alert alert-success" role="status">{message.success}</div>}<div className="field"><label htmlFor="email">Work email</label><input className="input" id="email" name="email" type="email" autoComplete="email" required /></div><div className="field"><div className="form-row"><label htmlFor="password">Password</label><Link href="/forgot-password">Forgot password?</Link></div><input className="input" id="password" name="password" type="password" autoComplete="current-password" required /></div><button className="button button-primary" type="submit">Sign in</button></form></section>; }
